{
  "mcpServers": {
    "hostinger": {
      "type": "stdio",
      "command": "npx",
      "args": ["-y", "hostinger-api-mcp@latest"],
      "env": {
        "HOSTINGER_API_TOKEN": "${HOSTINGER_API_TOKEN}"
      }
    }
  }
}
